= form_for @region, url: regions_path do |f|

  .field
    = f.label :name
    = f.text_field :name

  .field
    = f.label :image
    = f.text_field :image

  .field
    = f.label :description
    = f.text_area :description, rows: 10, cols: 60

  .actions
    = submit_tag('Create Region')
